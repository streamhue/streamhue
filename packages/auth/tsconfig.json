{
  "extends": "../../tsconfig.json",

  "compilerOptions":{
    "moduleResolution": "node",
    "baseUrl": ".",
    "outDir": "dist",
    "allowJs": true
  },

  "exclude": [
    "node_modules",
    "dist",
    "tests"
  ],

  "include": [
    "src/**/*"
]
}
